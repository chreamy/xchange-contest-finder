const router = require("express").Router();
router.route("/list").get(async (req, res) => {
  //get the contests by posts
});
router.route("/:id").get(async (req, res) => {
  // get the contests by ID
});
